#include <stdio.h>

int main() {
    int a=10;
    printf("enter a  num");
    scanf("%d",&a);
    if(a<10)
    {
        printf("A IS LESS THAN 10");
    }
    else
    {
        printf("not define");
    }
    return 0;
    }
